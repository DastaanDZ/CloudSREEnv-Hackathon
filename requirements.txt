openenv-core>=0.1.0
pydantic>=2.0,<3.0
openai>=1.30,<2.0
pandas>=2.0,<3.0
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pyyaml>=6.0

# --- Added for SFT/LoRA Training ---
torch>=2.0.0
transformers>=4.38.0
accelerate>=0.27.0
datasets>=2.16.0
peft>=0.10.0
matplotlib>=3.7.0

# --- Optional Colab training path for hackathon submission ---
# Unsloth has CUDA/Colab-specific wheels, so install it in Colab directly:
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# pip install --no-deps trl peft accelerate bitsandbytes